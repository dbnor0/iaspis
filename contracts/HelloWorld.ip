import "HelloNether.ip";
import "Ownable.ip";

open proxy Market for Primary, Secondary {
    @       * pub       bool test;
    @ Primary pvt const string x; 
}

facet Primary to Market {
    pub $ fn doSomething (address x) {
        body
    }
}

facet Secondary to Market {
    pvt ! proc doSomethingElse (string a, bytes b) {
        body
    }
}

struct test1 {
    string x;
    address y;
}

enum test2 {
    VALUE1,
    VALUE2
}